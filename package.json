{ "main": "index.js",
  "scripts": {
    "test": "mocha",
    "configure": "nodejs configure.js",
    "build": "node-gyp build",
    "postbuild": "nodejs postbuild.js",
    "preinstall": "nodejs preinstall.js",
    "install": "test -f build/Release/NativeExtension.node && exit 0; npm run configure; node-gyp rebuild; npm run postbuild" },
  "devDependencies": { "mocha": "^2.2.5" },
  "dependencies": { "bindings": "^1.2.1","nan": "^2.0.5","node-gyp": "^1.0.3" },
  "gypfile": true,
  "name": "tagscale",
  "description": "Key-value-store with secondary indices (tags) based on upscaledb",
  "version": "0.0.1",
  "directories": { "test": "test" },
  "repository": { "type": "git", "url": "https://github.com/haktor/node-tagscale" },
  "keywords": [ "upscaledb", "key", "value", "db", "secondary", "index" ],
  "author": "Sebastian Glaser <anx@ulzq.de>",
  "license": "GPLv3",
  "bugs": { "url": "https://github.com/haktor/node-tagscale/issues" }}
